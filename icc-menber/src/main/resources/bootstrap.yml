server:
  port: 9501
profiles:
  active: dev  # 开发、测试、生产环境、test、prop
spring:
  application:
    name: icc-menber # 服务名称
  cloud:
    nacos:
      server-addr: www.icc.com:80 # nginx反向代理nacos地址
      config:
        file-extension: yaml # 文件名后缀
      discovery:
        namespace: dev666


